---
import { toStaticPath } from './blog-routing';
import type { Post } from './blog-routing'; 

export async function getStaticPaths() {
  return (await Astro.glob<Post>('../_blog/*.{md,mdx}')).map(toStaticPath);
}

const { Content } = Astro.props.post;
---
<Content />
